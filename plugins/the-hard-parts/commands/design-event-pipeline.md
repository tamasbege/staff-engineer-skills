---
description: Design a production-grade event pipeline (launches event-pipeline-architect)
---

Invoke the `event-pipeline-architect` skill now and follow it exactly — starting with its output-format question (HTML default or Markdown) and its mandatory context-gathering phase (inspect the codebase first, then ask only what the code cannot answer).

Initial context provided by the user (may be empty): $ARGUMENTS
