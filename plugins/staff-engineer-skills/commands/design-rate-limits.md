---
description: Design rate limiting and quotas (launches rate-limiter-designer)
---

Invoke the `rate-limiter-designer` skill now and follow it exactly — starting with its output-format question (HTML default or Markdown) and its mandatory context-gathering phase (inspect the codebase first, then ask only what the code cannot answer).

Initial context provided by the user (may be empty): $ARGUMENTS
