---
description: Design a normalized API response contract (launches api-response-normalizer)
---

Invoke the `api-response-normalizer` skill now and follow it exactly — starting with its output-format question (HTML default or Markdown) and its mandatory context-gathering phase (inspect the codebase first, then ask only what the code cannot answer).

Initial context provided by the user (may be empty): $ARGUMENTS
