---
description: Design an expressive motion/interaction system (launches expressive-motion-architect)
---

Invoke the `expressive-motion-architect` skill now and follow it exactly — starting with its output-format question (HTML default or Markdown) and its mandatory context-gathering phase (platform(s), existing design system, personality target, the specific interaction in scope).

Initial context provided by the user (may be empty): $ARGUMENTS
